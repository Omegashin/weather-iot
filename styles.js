import { StyleSheet } from 'react-native';

// global colors
const red = 'green';

export default StyleSheet.create({
  alwaysred: {
    backgroundColor: red,
    height: 100,
    width: 100,
  },
});
